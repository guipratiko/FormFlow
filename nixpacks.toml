[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

# Easypanel define NODE_ENV=production; --include=dev garante tipos no build.
[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
